export enum PostRelation {
    Like = 'LIKE',
    In = 'IN',
    Publish = 'PUBLISH'
}

export enum CommentRelation {
    In = 'IN',
    Publish = 'PUBLISH'
}

export enum UserRelation {
    Friend = 'FRIEND',
    FriendRequest = 'FRIENDREQUEST'
}

export enum FeedRelation {
    Feed = 'FEED'
}

export enum GroupRelation {
    Members = 'MEMBER',
    Owner = 'OWNER'
}
